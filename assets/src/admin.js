/**
 * Entry point for the admin skin.
 *
 * The skin is pure CSS — it restyles WordPress's own markup and adds no
 * behaviour. This file exists because wp-scripts builds entries from JavaScript;
 * importing the stylesheet here is what emits build/admin.css.
 */

import './admin.scss';
