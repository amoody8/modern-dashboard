{
  "name": "modern-dashboard",
  "version": "0.6.0",
  "description": "Network-first admin dashboard for WordPress Multisite.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "wp-scripts build --webpack-src-dir=assets/src --output-path=build index.js palette.js admin.js shell.js",
    "start": "wp-scripts start --webpack-src-dir=assets/src --output-path=build index.js palette.js admin.js shell.js",
    "lint:js": "wp-scripts lint-js assets/src",
    "format": "wp-scripts format assets/src",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "env:start": "wp-env start",
    "env:setup": "bash bin/setup-dev-network.sh",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:cli": "wp-env run cli wp"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.2"
  },
  "devDependencies": {
    "@wordpress/env": "^10.39.0",
    "@wordpress/scripts": "^30.24.0",
    "typescript": "^5.9.3"
  }
}
